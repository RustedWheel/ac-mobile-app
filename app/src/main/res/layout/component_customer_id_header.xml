<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="@dimen/app_margin_half"
    android:paddingTop="@dimen/app_margin_2x_half"
    android:paddingEnd="@dimen/app_margin_half"
    android:paddingBottom="@dimen/app_margin_2x_half">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/customerCompanyName"
        style="@style/AC.Text.B1.Bold"
        tools:text="Bakerman"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/app_margin_2x"
        android:layout_weight="1"
        android:textColor="@color/ac_red_900" />

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/customerCompanyAccountNumber"
        style="@style/AC.Text.B1.Bold"
        tools:text="300-A003"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:textColor="@color/ac_orange_900" />

</LinearLayout>